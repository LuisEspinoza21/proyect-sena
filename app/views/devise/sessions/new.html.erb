<head> <%= stylesheet_link_tag "devise.css" %></head>
<body>
  <div class="main_devise">
    <main class= "inicio">
        <section id= section1>
            <article id=article1>
            <h2>Iniciar Sesión</h2>

            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="field">
                <%= f.label :Correo_Electronico %><br />
                <%= f.email_field :email, autofocus: true, autocomplete: "email"%>
              </div>
              <br>
              <div class="field">
                <%= f.label :Contraseña %><br />
                <%= f.password_field :password, autocomplete: "current-password" %>
              </div>
              
              <% if devise_mapping.rememberable? %>
                <div class="field">
                  <%= f.check_box :remember_me %>
                  <%= f.label :Recordar_contraseña  %>
                </div>
              <% end %>
              <br>
              <div class="actions">
                <%= f.submit "Iniciar Sesión" %>
              </div>
            <% end %>
            <%= render "devise/shared/links" %>
            </article>
        </section>
        <aside>
            <center>
            <div id="logo"><img id="imagen-logo" src="https://lostramites.com.co/wp-content/uploads/logo-sena-Naranja-300x300.jpg" alt="50"></div>
            
            <div id="nombre-empresa"><h3>Centro Industrial y De Aviacion</h3></div>
            <br>
            <div id="contactenos">Contactenos</div>
            </center>          
        </aside>
    </main>
    </div>
    <footer>
        <div id="info-footer">Version 1.0.0</div>
    </footer>
</body>
</html>
